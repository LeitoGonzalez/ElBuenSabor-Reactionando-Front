{/*import { useState } from "react";
import { DetalleCarrito } from "../../types/TypeDetalleCarrito";

  //Props
/*   type DetalleCarritoProps = {
    denominacion: string;
    precioVenta: ModalType;

    refreshData: React.Dispatch<React.SetStateAction<boolean>>;
} */}


const DetalleCarrito = (/* Producto, Servicio */) => {


/* 
    //Cantidad ede productos
    const [cantidad, setCantidad] = useState(0);
    const handleClickSuma = () =>{
        setCantidad(cantidad+1);
    }

    const handleClickResta = () =>{
        setCantidad(cantidad-1);
    }

  return (
    <>
      <button onClick={handleClickResta}>-</button>

      <button onClick={handleClickSuma}>+</button>
    </>
  ); */
};

export default DetalleCarrito;
